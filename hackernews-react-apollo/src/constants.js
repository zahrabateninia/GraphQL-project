// Storing JWTs in localStorage is NOT  a safe approach to implement authentication on the frontend. 
// Because I want to keep things simple I am using it here.
export const AUTH_TOKEN = 'auth-token';
